{
  "name": "@capsule/cli",
  "version": "0.0.1",
  "type": "module",
  "bin": {
    "capsule": "./bin/capsule.mjs"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "bin", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@capsule/core": "workspace:*",
    "@capsule/runtime": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.16.11"
  }
}
